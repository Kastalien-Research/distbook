# Sample doc

Intro paragraph cites `packages/api/srcmd/decoding.mts` for context.

## Section A

This claim mentions commit `c7a52cc` which is real.

But this clone does not include `deadbeef00000000`.

## Section B

PR #502 added the removal. See `docs/discovery/00-repo-map.md` for context.

References `packages/api/session.mts` and `packages/api/exec.mts`.
