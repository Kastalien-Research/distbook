# Boundary bug

This memo maps the checked-out repository at `fc57dd5`. Contrary to the prompt's public-main assumption, this checkout still contains app-builder code; the best local "last pre-removal app-builder state" is the checked-out `fc57dd5`.
